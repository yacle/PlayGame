<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<insert id="create" >
		insert into member values(#{id}, #{name}, #{password}, #{email}, #{pwd_hint})
	</insert> 
		
	<select id="read" resultType="com.playgame.domain.MemberVO">
		select * from member where id=#{id} and password=#{password}
	</select>
	<update id="update">
		update member set log_cnt=log_cnt+1 where id=#{id}
	</update>
	<delete id="delete">
		delete from member where id=#{id} and password=#{password}
	</delete>
	
</mapper>